import { SortByRolePipe } from './sort-by-pipe.pipe';

describe('SortByRolePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByRolePipe();
    expect(pipe).toBeTruthy();
  });
});
